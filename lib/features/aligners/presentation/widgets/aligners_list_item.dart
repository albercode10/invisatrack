import 'package:flutter/material.dart';
import 'package:invisatrack/features/aligners/domain/entities/aligner.dart';

class AlignersListItem extends StatelessWidget {
  final Aligner aligners;
  const AlignersListItem({Key? key, required this.aligners}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
